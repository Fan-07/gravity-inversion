# gravity-inversion
flips gravity for client -- why would you ever need this

1) starter gui structure:
ScreenGui
  └── MainFrame (Frame)
        ├── FlipButton (TextButton)  
        └── ResetButton (TextButton)  or image buttons

2) under starter player scripts, add the given script provided (as a local script)
